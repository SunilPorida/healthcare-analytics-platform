# Queries

Analytical SQL used by analysts, exports, or downstream tools (including Power BI when using DirectQuery or import from Snowflake).

Group by domain or report name. Parameterize warehouse, database, and schema via your SQL client or semantic layer.
